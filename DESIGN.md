# Design System Design Notes

## 定位

Design System 服务于企业应用平台的组件规范、交互示例和视觉一致性沉淀。当前站点采用静态 HTML 实现，适合作为设计验收、前端还原和组件扩展的共同参考。

## 设计目标

- 清晰：组件说明应帮助使用者快速判断何时使用、如何组合、有哪些边界状态。
- 稳定：基础 Token、组件尺寸和状态表达保持一致，降低跨页面理解成本。
- 高效：页面结构服务于后台工作台场景，信息密度适中，便于扫描和对照。
- 贴近业务：示例围绕 Agent 创建、任务配置、店铺运营、素材上传、时间排期等实际场景展开。

## 视觉语言

### 字体

- 全局字体统一使用 `Inter`。
- 字体栈必须以 `Inter`、`Inter var` 开头，中文字体作为后备，例如 `"PingFang SC"`、`"Microsoft YaHei"`。
- 全局字体栈不得显式使用 `SF Pro`，也不得通过 `system-ui`、`-apple-system`、`BlinkMacSystemFont` 等苹果系统字体别名回退到 SF Pro。
- 主题切换脚本如需覆盖 `font-family`，也必须保持 `Inter` 在首位。

### 色彩

- 品牌主色：`#00d157`
- 文字深色：`#0b0d0f`
- 中性色阶：从 `#14171a` 到 `#fafbfc`
- 成功态：沿用品牌绿色
- 危险态：`#d92d20`

色彩使用应优先服务于状态、层级和可读性。品牌色用于主按钮、关键状态提示，以及按钮、选择器、输入框等基础控件的 focused 描边；focused 态不叠加阴影；危险色用于错误、删除、失效等需要立即识别的反馈。侧导航列表的 hover 与选中态使用中性色层级，不使用品牌绿色。

### 夜间模式

夜间模式通过 Design Token / CSS Variables 实现，默认浅色主题。页面根节点使用 `data-theme` 控制主题状态：浅色为 `<html data-theme="light">`，深色为 `<html data-theme="dark">`。组件不得复制两套 DOM 或页面，也不得通过 `filter`、`invert`、整体透明度等方式反转图片、视频、Logo 或头像。

主题颜色必须优先使用语义变量，组件内部避免大量硬编码颜色。当前页面级语义变量包括：

- `--color-bg-page`：页面主背景。
- `--color-bg-header`：顶部导航背景。
- `--color-bg-sidebar`：一级导航背景。
- `--color-bg-surface`：卡片、弹窗、输入框等主要承载面。
- `--color-bg-subtle`：弱背景、列表 hover、轻量分区背景。
- `--color-bg-selected`：导航、菜单、选项等选中态背景。
- `--color-text-primary`：正文与主要标题。
- `--color-text-secondary`：辅助说明、元信息、未选中标签。
- `--color-text-disabled`：禁用文字。
- `--color-border-default`：默认描边与分割线。
- `--color-border-subtle`：弱描边。
- `--color-brand-primary`：品牌主色。
- `--color-brand-hover`：品牌按钮 hover。
- `--color-brand-active`：品牌按钮 active。
- `--color-status-success-bg`：成功/Short 标签等低饱和绿色背景。
- `--color-mask`：弹窗遮罩。
- `--color-shadow`：阴影颜色。

深色主题视觉规则：

- 页面背景使用深灰，不使用纯黑；顶部导航、一级导航、内容卡片、弹窗和输入框应使用不同深色层级区分区域。
- 正文文字需保持高对比度浅色，辅助文字使用中灰色，禁用文字进一步降低对比度；正文与背景至少满足 WCAG AA。
- 边框在深色背景下必须可见但不过亮，卡片优先通过背景层级与轻描边区分，阴影应降低存在感。
- 品牌绿色保持品牌识别，不做简单反相；主按钮如 `Upload`、`Publish`、`Republish` 继续使用品牌绿色，并提供 hover、active、disabled 状态。
- 次要按钮如 `Undo Changes` 使用深色表面背景、可见描边和浅色文字；hover 态使用深色弱背景，不出现白色底。
- 输入框、文本域、选择器、搜索框在深色模式下使用深色表面背景；默认、hover、focus、disabled、error 状态均需清晰区分；focus 继续使用品牌绿色描边且不叠加阴影。
- 顶部导航、侧导航、Content menu、Episode tab、筛选下拉、标签、弹窗、Tooltip、Popover、Loading 均必须适配深色背景与文字。
- Content menu 选中态使用深色高亮背景，不直接沿用浅色模式浅灰；获焦态不额外增加底色。
- Episode 选中项继续使用品牌绿色下划线，未选中文字使用 `--color-text-secondary`。
- Tags 标签选中态在深色模式下使用低饱和深绿色背景和清晰绿色文字，未选中标签使用深色表面背景与可见描边。
- Short 标签使用低饱和深绿色背景与绿色文字；`Online`、`Offline`、`Draft` 发布状态标签继续无底色，仅通过图标和文字颜色表达状态。
- 封面、视频预览和媒体内容本身不进行颜色反转，只调整容器背景、边框、说明文字和操作按钮状态。
- 可横向滚动的导航、Episode 标签栏和封面列表在深色模式下不得出现默认粗滚动条；需要隐藏滚动条时仍应保留滚动能力。
- 夜间模式不得新增页面级横向滚动，不改变组件尺寸、布局位置和既有响应式断点。

主题切换入口位于顶部导航右上角，语言入口左侧。按钮最小点击区域为 `40px × 40px`，浅色模式显示月亮图标，深色模式显示太阳图标；必须提供 `aria-label`、`title`、hover、active 和 keyboard focus 样式。首次访问优先读取 `prefers-color-scheme: dark`，用户手动切换后写入 `localStorage`，刷新后恢复选择，并通过首屏内联初始化避免先闪浅色再切深色。

### 布局

- Studio 首页顶部栏高度：`64px`，通栏固定展示。
- Studio 首页侧边导航宽度：`240px`，固定在顶部栏下方。
- 顶部栏左侧使用 `logo.png` 作为 iQIYI Logo 图片资源，Logo 显示高度为 `24px`，宽度按图片原始比例自适应；Logo 后展示 `Studio` 标题。
- 顶部栏右侧展示头像图片，头像按钮尺寸为 `36px × 36px`。
- 主内容区采用左侧导航加右侧画布的工作台布局。
- Content 区域默认使用更浅的灰色页面背景 `#fafbfc` 承载内容，标题、说明和主要模块直接平铺在内容背景上，不默认增加白色圆角底 card。
- Content 主内容区内边距为 `28px`；移动端可收窄为 `20px 16px`。
- Content 标题区由主标题和副标题组成一个 frame，主标题与副标题间距为 `8px`。
- Content eyebrow 与标题 frame 之间保持 `12px` 间距。
- 只有重复列表项、数据卡片、弹窗、浮层、表单分组或需要明确聚合边界的模块，才使用独立白色 card。
- 文档内容以组件说明、示例块、属性表和 Token 表为主要结构。

### 浮层层级

下拉菜单、级联菜单、自动补全候选、日期时间面板、Tooltip、Popover 等浮层必须作为触发控件的 `+1` 层级呈现，不参与原底层 card、表单行或页面区块的高度计算：

- 浮层容器应使用 `position: absolute` 或等价的 portal/overlay 机制，并设置明确的 `z-index`，相对触发控件悬浮展示。
- 触发控件所在 card 的默认高度不得因为浮层展开而增加；展开和收起时，底层布局、相邻 card 与页面滚动位置应保持稳定。
- 文档示例如果需要展示默认打开状态，只能为演示区域预留固定高度或使用独立预览画布，不得依赖浮层内容把 card 撑高。
- 下拉类组件默认应收起；除非组件规范明确说明“默认展开演示”，否则页面加载时不应自动选中或展开。
- 下拉类浮层展开后，点击触发控件和浮层内容以外的页面区域必须自动收起；组件内部点击不应误触发收起。
- 浮层超出容器时优先通过更高层级、裁切规避或 portal 处理，不通过增加父级 padding、margin、min-height 来临时修补。

### 全局侧导航

侧导航是所有 HTML 页面共享的全局结构，新增或调整组件页时必须保持一致：

- Studio 首页侧导航使用 `240px` 宽度；文档型组件页如需保持组件目录结构，可继续使用 `300px` 宽度。
- Studio 首页侧导航顶部优先展示 `Upload` 主按钮，按钮使用品牌绿色和上传 Icon，作为主要行动入口。
- `Upload` 按钮与下方导航列表之间的间距为 `24px`。
- Studio 首页导航列表项包含 `Dashboard`、`Content`、`Analytics`、`Subtitles`，每项均包含 Icon。
- 侧导航列表项之间不留间距，`row-gap` 与 `column-gap` 均为 `0`。
- 侧导航列表项 hover 背景使用中性浅灰 `#f2f4f7`，文字使用深色 `#0b0d0f`。
- 侧导航列表项选中态背景使用中性灰 `#e8ebee`，文字使用深色 `#14171a`。
- 全局 tab、侧导航项、选项型 tab 及常规界面文字字重统一为 `500`；选中态通过颜色、背景或下划线形成层级，不额外提高字重。
- 侧导航列表项 hover 与选中态不得使用品牌绿色，也不得叠加描边、内描边或 inset border。
- 所有导航项和分组文案统一使用英文在前、中文在后，例如 `Styles 全局样式`、`Components 组件总览`、`Data Entry 数据录入`、`AutoComplete 自动补全`。
- 所有页面侧导航宽度、字号、行高和间距保持一致：桌面端侧栏宽度 `300px`，导航项最小高度 `40px`，导航文字字号 `14px`，分组文字字号 `12px`。
- 侧导航中的当前页必须通过 `aria-current="page"` 标记，便于视觉高亮、辅助技术识别和脚本定位。
- 页面加载后，当前选中的导航 tab 应自动滚动到侧导航可视区域中部。桌面端按垂直方向居中；移动端或横向侧导航按横向方向居中。
- 页面布局必须固定在视口高度内，让侧导航自身成为滚动容器；不得依赖 `body` 滚动承载侧导航，否则靠后的 tab 无法稳定自动居中。
- 侧导航列表底部需要保留约半屏高度的滚动缓冲，确保 `Cascader 联级选择` 及其下方 tab 也能滚动到可视区域中部。
- 侧导航滚动条必须与主模板保持一致：使用细滚动条、透明轨道、中性色滑块和稳定 gutter，独立组件页不得使用浏览器默认粗滚动条。
- 首页 `index.html` 的 `Styles` 与 `Components` 使用 `data-page` 切换面板，其他独立组件页使用链接跳转，但展示文案、尺寸和选中态行为必须一致。

### 圆角与聚焦

- 基础圆角：`6px`
- 焦点阴影：不使用，focused 态不叠加 `box-shadow`

交互控件需要提供清晰的 hover、focus、active、disabled 和 error 表达，焦点态应稳定可见，避免只依赖颜色变化。

### 按钮状态

- Default：普通次级按钮、图标按钮、选择器触发器和输入框默认使用白色背景 `#ffffff`，轻量描边 `#edf0f2`，文字使用深色或中性色；不使用灰色底。
- Hover：灰色背景仅用于 hover 态，推荐使用 `#f6f7f8`；hover 态可同步加深文字颜色，但不改变为激活态样式。
- Focused：focused 态底色保持白色 `#ffffff`，描边使用品牌绿色 `#00d157`；所有按钮、选择器和输入框在 focused 态不得使用灰色底，也不得叠加阴影。
- Active / Open：按钮、选择器、输入框处于激活或展开状态时，底色仍保持白色；选择器和输入框应沿用 focused 态的品牌绿色描边。
- 深色模式下，以上白色背景规则应映射为 `--color-bg-surface`，hover 映射为 `--color-bg-subtle`，描边映射为 `--color-border-default`；主按钮仍使用品牌绿色。

### 发布状态标签

发布状态标签用于内容列表、筛选下拉和详情预览中的 `Online`、`Offline`、`Draft` 状态展示。标签应保持轻量，不使用填充底色或描边，通过图标与文字颜色表达状态；图标尺寸为 `16px × 16px`，文字字号 `12px`，行高 `16px`，字重 `500`，图标与文字间距 `4px`。

- Online：背景 `transparent`，文字和图标使用品牌绿色；浅色模式推荐 `#00a846`，深色模式推荐 `#73f2a0`。
- Offline：背景 `transparent`，文字和图标使用橙色提示色；浅色模式推荐 `#9a4d0f`，深色模式推荐 `#f3b95f`。Offline 状态不得使用底色。
- Draft：背景 `transparent`，文字和图标使用中性辅助色；浅色模式推荐 `#52525b`，深色模式推荐 `#aab4bf`。

### 描边与分割线

- 全局描边应保持克制，默认分割线颜色使用 `#edf0f2`。
- 顶部栏底部分割线、侧边导航右侧分割线和移动端导航底部分割线统一使用 `1px solid #edf0f2`。
- 头像、输入框、按钮等轻量控件如需描边，优先使用 `#edf0f2` 或同等明度的浅灰色。
- Content 主画布默认使用 `#fafbfc` 背景，不使用白色圆角 card、描边或阴影；内容直接平铺在灰色背景上。
- 当具体模块确实需要形成独立承载面时，优先使用背景层级或轻阴影表达边界，避免使用过强描边；参考阴影为 `0 1px 2px rgba(20, 23, 26, 0.04)`。
- 侧导航选中态、列表 hover 态不额外叠加描边。

### 滚动条

- 细滚动条宽度：`4px`
- 滚动条轨道：`transparent`
- 滚动条滑块：`var(--gray-300)`
- 滚动条圆角：`999px`

下拉菜单、浮层列表和组件内部滚动区域默认使用细滚动条。滚动条轨道不应出现底色，避免在轻量浮层中形成额外边界；仅显示中性色滑块，并在需要滚动时提供足够可见性。

## 组件组织

当前组件按四类组织：

- 通用：Button
- 布局：Divider、Space、Grid、Layout、Splitter
- 数据录入：Input、InputNumber、Radio、Checkbox、Switch、Select、DatePicker、TimePicker、Upload、Form、AutoComplete、Cascader、Mentions、VerficationCode、Rate、Slider
- 数据展示：Card、Table

完整组件规范建议包含以下章节：

- 何时使用：说明组件适用场景和不适用边界。
- 基础用法：展示默认结构和最常见用法。
- 组合类型或布局：说明复合场景、变体和信息组织方式。
- 状态：覆盖默认、悬停、聚焦、选中、错误、禁用、加载等状态。
- 尺寸：说明小、中、大或业务约定尺寸。
- API：列出属性、说明、类型、默认值。
- 设计 Token：列出组件私有或共享样式变量。

## 交互原则

- 表单反馈应靠近字段，帮助用户快速定位问题。
- 选择类组件应明确当前值、展开状态、选项状态和清除操作。
- 日期与时间组件应同时照顾单值、范围、空状态和错误状态。
- 上传组件应展示文件队列、进度、成功、失败和删除能力。
- 按钮应根据任务优先级区分主按钮、次按钮、文本按钮、危险按钮和加载状态。

## 内容风格

文案保持简洁、明确、业务化。示例中优先使用 企业应用平台相关对象，例如 Agent、任务、店铺、素材、授权、回调地址、排期和批量操作参数。

## 扩展建议

新增组件时，先补齐组件入口和全局侧导航，并复用统一的侧导航文案顺序、尺寸规则和选中项自动居中逻辑，再补齐规范章节。若组件有复杂交互，应提供可操作示例；若组件仅作为展示规范，也应包含关键状态和 Token 表，确保设计与实现可以对齐。
